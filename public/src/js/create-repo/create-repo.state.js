'use strict';

module.exports = class Repository {
  
  constructor(user,repoName,repoDescription,repoType) {
    this.user = user;
    this.repoName = reoName,
    this.repoDescription = repoDescription,
    this.repoType = repoType
  }

}
